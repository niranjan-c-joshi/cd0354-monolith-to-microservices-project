apiVersion: v1
kind: Service

metadata:
  name: reverseproxy
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing

spec:
  selector:
    app: reverseproxy

  ports:
  - port: 8080
    targetPort: 8080

  type: LoadBalancer